import { I18nValidationExceptionFilter } from './i18n-validation-exception.filter';

describe('I18nValidationExceptionFilter', () => {
  it('should be defined', () => {
    expect(new I18nValidationExceptionFilter()).toBeDefined();
  });
});
